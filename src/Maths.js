function add(x, y) {
	return x + y;
};

function subtract(x, y) {
	return x - y;
};

function multiply(x, y) {
	return x * y;
};

function divide(x, y) {
	return x / y
};

function square(x) {
	return x * x
};

function sqRoot(x) {
	return Math.sqrt(x);
};